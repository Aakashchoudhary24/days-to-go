1. use shadcn for all components/elements using this specific configuration command -> pnpm dlx shadcn@latest init --preset b2pQWn7C4 --template next
2. left navigation bar should be centred, like an island
3. add a theme toggle mode -> light and dark -> make the text on light mode much darker/blacker... currently it's mostly gray
4. on mobile view the navigation appears on the bottom -> remove that and make it a hamburger menu next to the "+" button on top right
5. add progress bar for each task now, I wanna be able to see completion. Grows as I am near the end date.
